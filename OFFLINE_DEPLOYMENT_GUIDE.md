# دليل النشر والتشغيل أوفلاين وتصدير تطبيق أراسكو (Arasko Offline Deployment & PWA Guide)

يقدم هذا الدليل خطوات مفصلة وشاملة لتحويل تطبيق **أراسكو (Arasko)** إلى تطبيق ويب تقدمي مستقل (Standalone PWA) وتصديره إلى حزمة هاتف أندرويد (APK / AAB) أو رفعه على موقعك وقناتك ليعمل بالكامل بدون الحاجة إلى اتصال بالإنترنت (100% Offline-First).

---

## 1. المعمارية الأوفلاين في أراسكو (Offline-First Architecture)

تم بناء أراسكو وفق المعايير التالية لضمان عمل كافة الوظائف دون إنترنت:
1. **تخزين محلي ذري (Atomic LocalStorage Engine)**: حفظ فوري لجميع المهام، السجلات، وتفضيلات المستخدم مع نظام معالجة استثنائي للحصص (`QuotaExceededError`)، واسترجاع تلقائي للبيانات عبر الذاكرة السريعة (`In-Memory Safety Cache`).
2. **عامل الخدمة (Service Worker - `sw.js`)**: تخزين ملفات الـ HTML والـ JavaScript والـ CSS والخطوط والأيقونات محلياً على جهاز المستخدم باستخدام استراتيجية `Cache-First` مع تحديث `Stale-While-Revalidate` في الخلفية.
3. **ملف الإعدادات والمانيفست (`manifest.json`)**: تعريف التطبيق كتطبيق شاشة كاملة مستقل (`standalone`) مع إمكانية التثبيت المباشر على الشاشة الرئيسية للهواتف وأجهزة الكمبيوتر.
4. **محرك أصوات وتوليد ذبذبات متكامل**: جميع أصوات المنبهات ومؤقت بومودورو يتم إنشاؤها وتوليدها برمجياً عبر `Web Audio API` مدمجة دون الحاجة إلى تحميل ملفات وسائط خارجية.

---

## 2. خطوات البناء وتجهيز حزمة الإنتاج (Production Build)

لتجهيز ملفات التطبيق للنشر النهائي:

```bash
# 1. تثبيت الاعتماديات
npm install

# 2. فحص الأخطاء البرمجية والأنماط
npm run lint

# 3. بناء النسخة المصغرة للإنتاج (Output into /dist)
npm run build
```

بعد انتهاء عملية البناء، ستجد كافة الملفات المجهزة داخل مجلد `dist/` وهي ملفات ثابتة نقية (Static Assets) يمكن استضافتها على أي خادم ويب (Nginx, Apache, Cloudflare Pages, Vercel, Netlify, GitHub Pages, Firebase Hosting) أو تضمينها في حزمة APK.

---

## 3. خطوات تحويل التطبيق إلى APK أصلي للهاتف (Converting to Android APK)

يمكنك بسهولة تحويل مجلد `dist/` أو رابط موقعك إلى تطبيق أندرويد APK قابل للنشر على قناتك وموقعك باستخدام أحد الطرق التالية:

### الطريقة الأولى: استخدام Capacitor (الخيار الموصى به لتطبيقات الإنتاج)
1. داخل المجلد الرئيسي للتطبيق، قم بتثبيت Capacitor:
   ```bash
   npm install @capacitor/core @capacitor/cli @capacitor/android
   npx cap init Arasko com.fallt.arasko --web-dir dist
   ```
2. قم بإضافة منصة الأندرويد ومزامنة ملفات البناء:
   ```bash
   npm run build
   npx cap add android
   npx cap copy android
   ```
3. افتح المشروع في Android Studio:
   ```bash
   npx cap open android
   ```
4. من القائمة العلوية في Android Studio: اختر **Build > Build Bundle(s) / APK(s) > Build APK(s)** لتوليد ملف الـ APK النهائي القابل للتثبيت مباشرة على الهواتف.

### الطريقة الثانية: استخدام PWABuilder (أسرع طريقة أونلاين)
1. ارفع ملفات التطبيق على استضافتك أو موقعك عبر HTTPS.
2. توجه إلى الموقع الرسمي [PWABuilder.com](https://www.pwabuilder.com).
3. أدخل رابط موقعك واضغط **Start**.
4. سيتحقق الموقع من توافر `manifest.json` و `sw.js` (المضمنين في المشروع بالفعل).
5. اضغط **Package for Android** لتوليد ملف APK جاهز للتنزيل والنشر فوراً.

---

## 4. التحقق من توافق وتثبيت الـ PWA على مختلف الأجهزة

- **على هواتف أندرويد (Chrome / Edge / Samsung Internet)**:
  - عند فتح التطبيق، يظهر شريط تلقائي أسفل الشاشة: **"إضافة أراسكو إلى الشاشة الرئيسية"**.
  - أو من قائمة المتصفح (3 نقاط) > اختيار **"تثبيت التطبيق" (Install App)**.
- **على هواتف آيفون (iOS Safari)**:
  - الضغط على زر المشاركة (Share Icon) > التمرير واختيار **"إضافة إلى الصفحة الرئيسية" (Add to Home Screen)**.
- **على الحواسيب (Windows / Mac / Linux)**:
  - تظهر أيقونة التثبيت بجوار شريط العنوان في المتصفح لتثبيته كبرنامج مستقل على سطح المكتب.

---

## 5. النسخ الاحتياطي واستعادة البيانات للإنتاج

يحتوي التطبيق على نظام تصدير واستيراد للبيانات بصيغة `JSON` متوفر في **الإعدادات > إدارة البيانات والنسخ الاحتياطي**، مما يتيح للمستخدمين:
- حفظ نسخة احتياطية من كافة المهام وسجلات الصحة والأذكار بضغطة زر واحدة.
- نقل البيانات بين الهواتف والمتصفحات دون الحاجة لأي خادم وسيط.
- استعادة البيانات فورياً مع التحقق التلقائي من سلامة البيانات والـ Schema.
